import frameSizeConfig from '../../config/frameSizeConfig'
import OverlayLayout from '../ui/OverlayLayout'

export default function Catalog() {
  return <OverlayLayout frame={frameSizeConfig.stoneCatalog} />
}
